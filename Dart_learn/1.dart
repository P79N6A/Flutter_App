//定义打印数字方法
printNumber(num Number) {
  print("The number is $Number");
}

//执行程序入口
void main() {
  //定义初始化一个变量
  var number;
  int _status = 0;
  print((_status == 1));

  //调用打印数字方法
  printNumber(number);
}
